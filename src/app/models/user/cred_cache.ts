export class CredCache {
    constructor(
        public username: string, 
        public isAdmin:boolean, 
        public clientId: string, 
        public name: string, 
        public surname){}
}